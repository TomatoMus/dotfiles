# $Id: vim-keys.conf,v 1.2 2010-09-18 09:36:15 nicm Exp $
#
# vim-keys.conf, v1.2 2010/09/12
#
# By Daniel Thau.  Public domain.
#
# This configuration file binds many vi- and vim-like bindings to the
# appropriate tmux key bindings.  Note that for many key bindings there is no
# tmux analogue.  This is intended for tmux 1.3, which handles pane selection
# differently from the previous versions

# split windows like vim
# vim's definition of a horizontal/vertical split is reversed from tmux's
# bind s split-window -v
# bind v split-window -h


# resize panes like vim
# feel free to change the "1" to however many lines you want to resize by, only
# one at a time can be slow
# bind < resize-pane -L 1
# bind > resize-pane -R 1
# bind - resize-pane -D 1
# bind + resize-pane -U 1

# bind : to command-prompt like vim
# this is the default in tmux already
bind : command-prompt

# Session   Project
# Window    Tab
# Pane      Terminal

###
### 基本設定
###

# prefixキーをC-tに変更する
set -g prefix C-t
unbind C-b
# なんらかのアプリでC-tを使用するためにC-tを２回打つと有効にする
bind C-t send-prefix

# 設定ファイルのリロード
bind r source-file ~/.tmux.conf \; display "Configuration Reloaded!"

# True Color
set -g default-terminal "xterm-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

###
### Vim
###

# Vi キーバインド
set-window-option -g mode-keys vi
setw -g mode-keys vi

# vimのカラースキームが反映されない
# set-option -g default-terminal screen-256color
# set -g terminal-overrides 'xterm:colors=256'

###
### ステータスバー
###

# ステータスバーをトップに配置する
# set-option -g status-position top

# 左右のステータスバーの長さを決定する
set-option -g status-left-length 90
set-option -g status-right-length 90

# #H => マシン名
# #P => ペイン番号
# 最左に表示
set-option -g status-left '#H:[#P]'

# Wi-Fi、バッテリー残量、現在時刻
# 最右に表示
set-option -g status-right '#(battery -c tmux) [%Y-%m-%d(%a) %H:%M]'

# ステータスバーを Utf-8 に対応
# set-option -g status-utf8 on

# ステータスバーを1秒毎に描画し直す
set-option -g status-interval 1

# センタライズ(主にウィンドウ番号など)
set-option -g status-justify centre

###
### コピーモード
###

# Prefix+v でコピーモード開始
bind-key v copy-mode \; display "Copy mode!"
bind-key -t vi-copy v begin-selection

# Prefix+y でコピー
# マウスでコピーする場合はOptionを押しながら選択
# bind-key -t vi-copy y     copy-pipe "reattach-to-user-namespace pbcopy"
bind-key -t vi-copy Enter copy-pipe "cat | xsel -i --clipboard"

# Prefix+p でペースト
# クリップボードにも保存されているので Cmd-v でもペースト可能
bind-key p paste-buffer

# vim <=> tmux 間でクリップボード利用を可能にする
# set-option -g default-command "reattach-to-user-namespace -l $SHELL"
set-option -g default-command "reattach-to-user-namespace -l zsh"

# Esc キーでコピーの反転を解除(コピーモードは抜けない)
bind-key -t vi-copy Escape clear-selection
# Ctrl-c キーでコピーの反転を解除し、コピーモードから抜ける
bind-key -t vi-copy C-c   cancel

# コピーモード中(Prefix+v 後)C-v で矩形選択開始
bind-key -t vi-copy C-v   rectangle-toggle
# 1行選択
bind-key -t vi-copy V    select-line

# Vi モード中に Ctrl-a で行頭に(Emacs ライク)
bind-key -t vi-copy C-a   start-of-line
# Vi モード中に Ctrl-e で行末に(Emacs ライク)
bind-key -t vi-copy C-e   end-of-line

# 単語の最初の1文字に移動
bind-key -t vi-copy w    next-word
# 単語の最後の1文字に移動
bind-key -t vi-copy e    next-word-end
# w の逆の動き back
bind-key -t vi-copy b    previous-word

# 画面上に映る最上行に移動
bind-key -t vi-copy g    top-line
# 画面上に映る最下行に移動
bind-key -t vi-copy G    bottom-line

# 前方検索
bind-key -t vi-copy /    search-forward
# 後方検索
bind-key -t vi-copy ?    search-backward

# ページスクロール
bind-key -t vi-copy C-n   page-up
bind-key -t vi-copy C-f   page-down
# ページ送り
bind-key -t vi-copy C-u   scroll-up
bind-key -t vi-copy C-d   scroll-down


###
### マウス設定
###

# マウス設定
set-option -g mouse on
bind -n WheelUpPane   select-pane -t= \; copy-mode -e \; send-keys -M
bind -n WheelDownPane select-pane -t= \;                 send-keys -M



###
### セッション＞ウィンドウ＞パネル
###

# ウィンドウとペインの番号を1から開始する(デフォルト0)
set-option -g base-index 1
set-window-option -g pane-base-index 1

# Prefix+- で横に、Prefix+| で縦に分割(ペイン)する
bind-key | split-window -h
bind-key - split-window -v

# Prefix + Ctrl-h/l でウィンドウ切り替え
# Prefix + Ctrl-h,h,h,h,...と連打できる
bind-key -r C-h select-window -t :-
bind-key -r C-l select-window -t :+

# Prefix+hjkl でペイン移動
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# ペインサイズを上下左右(Prefix+JKHL)に変更
# Prefix+J,J,J,J,...と連打してリサイズ可能
bind-key -r H resize-pane -L 5
bind-key -r J resize-pane -D 5
bind-key -r K resize-pane -U 5
bind-key -r L resize-pane -R 5

# Ctrl-o でペインをローテーションしながら移動
# Prefix を用いないのでタイプが楽だが、Ctrl-o を使用してしまう
# 他のソフトウェアの設定に支障をきたさないように注意
bind-key -n C-o select-pane -t :.+

# 画面分割と新規ウィンドウでカレントパスを引き継ぐ
bind c new-window -c "#{pane_current_path}"
bind | split-window -hc "#{pane_current_path}"
bind - split-window -vc "#{pane_current_path}"
tmux_conf_new_panes_retain_current_path=true

# tpm
set -g @tpm_plugins '              \
  tmux-plugins/tpm                 \
  tmux-plugins/tmux-yank           \
  tmux-plugins/tmux-open           \
  tmux-plugins/tmux-resurrect      \
  tmux-plugins/tmux-battery        \
  tmux-plugins/tmux-pain-control   \
  tmux-plugins/tmux-cpu            \
'
set -g @plugin 'jimeh/tmux-themepack'
set -g @themepack 'double/cyan'

run-shell '~/.tmux/plugins/tpm/tpm'

# tmux-themepack
set -g status-bg "#1d1f21"
# Prefix key が押されているかどうかでleftが反転#{?client_prefix,#[reverse],}
# set -g status-left "#[fg=colour232,bg=colour24,bold]#{?client_prefix,#[reverse],}   #S #[fg=colour24,bg=colour240,nobold]#[fg=colour233,bg=colour240] #(whoami) #[fg=colour240,bg=colour235]#[fg=colour240,bg=colour235]   #I:#P #[fg=colour235,bg=colour233,nobold]"
set -g status-left "#[fg=#b3deef,bg=#0077C0]   #S #[fg=#0077C0,bg=#C7EEFF,nobold]#[fg=#0077C0,bg=#C7EEFF]   #I:#P #[fg=#C7EEFF,bg=#1d1f21,nobold]"

# set -g status-right "#[fg=colour235,bg=colour233]#[fg=colour240,bg=colour235] %H:%M:%S #[fg=colour240,bg=colour235]#[fg=colour233,bg=colour240] %d-%b-%y #[fg=colour24,bg=colour240]#[fg=colour232,bg=colour24,bold] #H "
set -g status-right "#[fg=#C7EEFF,bg=#1d1f21]#[fg=#0077C0,bg=#C7EEFF] %H:%M:%S #[fg=#0077C0,bg=#C7EEFF]#[fg=#C7EEFF,bg=#0077C0] %d-%b-%y "

# set -g window-status-current-format "#[fg=colour233,bg=black]#[fg=colour232,bg=colour24,nobold] #I:#W#F #[fg=colour233,bg=black,nobold]"
set -g window-status-current-format "#[fg=#0077C0,bg=#1d1f21]#[fg=#C7EEFF,bg=#0077C0,nobold] #I:#W#F #[fg=#0077C0,bg=#1d1f21,nobold]"

